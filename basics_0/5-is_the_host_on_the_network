#!/usr/bin/env bash
# Ping the provided IP address five times

set -euo pipefail

if [ "$#" -ne 1 ] || [ -z "${1:-}" ]; then
	echo "Usage: 5-is_the_host_on_the_network {IP_ADDRESS}"
	exit 1
fi

ping -c 5 "$1"
