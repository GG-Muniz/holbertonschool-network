 second project #!/usr/bin/env bash
# Listen on TCP port 98 on localhost and echo received data

set -euo pipefail

PORT=98
HOST=127.0.0.1

if command -v nc >/dev/null 2>&1; then
	nc -l "$HOST" "$PORT"
else
	netcat -l "$HOST" "$PORT"
fi
